# Vishnu configuration server properties
server:
  port: 8888

spring:
  cloud.config.server:
        git:
          uri: https://github.com/fridayy/vihsnu-config
  application:
    name: vishnu-config

eureka:
  instance:
    preferIpAddress: true