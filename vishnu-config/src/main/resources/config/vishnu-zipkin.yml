spring:
  cloud:
    config:
      name: vishnu-zipkin
      discovery:
        service-id: config
        enabled: true

server:
  port: 9411


eureka:
  instance:
    preferIpAddress: true


logging:
  level:
    org:
      springframework:
        web: debug